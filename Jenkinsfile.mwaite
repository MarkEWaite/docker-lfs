pipeline {
    agent {
        label 'docker'
    }

    stages {
        stage('Hello') {
            steps {
		// sshagent(['MarkEWaite-github-rsa-private-key-from-mark-pc2']) {
		    sh 'ls -al ref/plugins && ./docker_build.py'
		// }
            }
        }
    }
}
