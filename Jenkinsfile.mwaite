pipeline {
    agent {
        label 'docker'
    }

    stages {
        stage('Hello') {
            steps {
		sshagent(['mark.earl.waite-gmail.com-ed25519-private-key-with-passphrase']) {
		    sh 'ls -al ref/plugins'
		}
            }
        }
    }
}
